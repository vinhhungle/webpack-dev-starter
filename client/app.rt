<!DOCTYPE html>
<div>
  <h1>{this.props.title}</h1>
  <button onClick="(ev) => this.handleClick(ev)">Click Me</button>
  <p rt-if="this.state.open" key="0">This is my paragraph. It opens and closes by <em>rt-if</em> directive.</p>
</div>